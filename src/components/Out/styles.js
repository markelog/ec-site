import { cyan } from '../../variables/colors';

const styles = {
  head: {
    color: '#fff',
    fontWeight: 'bolder',
    display: 'inline-block',
    fontSize: '14px',
  },

  text: {
    fontWeight: 'bolder',
    color: cyan,
    clear: 'right',
    fontSize: '14px',
  }
};

export default styles;
